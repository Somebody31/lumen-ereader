# Optional local secrets for wrangler dev with bindings.
# Production: use `wrangler secret put` instead.

# SESSION_SECRET=generate-a-long-random-string
# READER_PASSPHRASE=your-private-passphrase

# DeepSeek V4 Flash — required for CN→EN translation. Never commit a real key.
# DEEPSEEK_API_KEY=sk-...
